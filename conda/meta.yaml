{% set name = "redis-ipc" %}
{% set version = "0.0.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  path: ..

build:
  number: 0
  skip: true  # [win]
  run_exports:  # [not win]
    - {{ pin_subpackage('redis-ipc', max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - pkg-config  # [unix]
    - make  # [unix]
    - json-c
    - libhiredis

  host:
    - redis-py
    - json-c
    - libhiredis
    - pthread-stubs

test:
  requires:
    - pkg-config
    #- conda-build
  commands:
    {% set libname = "libredis_ipc" %}
    - ldd ${PREFIX}/lib/{{ libname }}${SHLIB_EXT}  # [linux]
    - test -f ${PREFIX}/include/redis_ipc.h  # [unix]
    - test -f ${PREFIX}/lib/{{ libname }}${SHLIB_EXT}  # [osx]
    - test -f ${PREFIX}`pkg-config --variable=libdir --dont-define-prefix {{ name }}`/{{ libname }}$SHLIB_EXT  # [unix]
    #- conda inspect linkages -p $PREFIX $PKG_NAME  # [unix]
    #- conda inspect objects -p $PREFIX $PKG_NAME  # [osx]
    - echo "TODO - Pkg tests require redis server."

about:
  home: https://github.com/VCTLabs/redis-ipc
  license: GPL-2.0-only
  license_family: GPL
  license_file: LICENSE
  summary: advanced IPC mechanism using redis

extra:
  recipe-maintainers:
    - sarnold
