{% set name = "redis-ipc" %}
{% set version = "0.0.3" %}
{% set soversion = "0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  path: ..

build:
  number: 0
  skip: true           # [win and float(vc) < 14]
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - cmake
    - pkg-config
    - make  # [unix]
    - json-c
    - libhiredis

  host:
    - msinttypes  # [win and float(vc) < 14]
    - redis-py

test:
  commands:
    - ldd ${PREFIX}/lib/libredis_ipc.so  # [linux]
    - test -f ${PREFIX}/include/redis_ipc.h  # [unix]
    - test -f ${PREFIX}/lib/libredis_ipc.{{ soversion }}${SHLIB_EXT}  # [osx]
    - if not exist %PREFIX%\\Library\\include\\redis_ipc.h exit 1  # [win]
    - echo "TODO - Pkg tests require redis server."

about:
  home: https://github.com/VCTLabs/redis-ipc
  license: GPL-2.0-only
  license_family: GPL
  license_file: LICENSE
  summary: advanced IPC mechanism using redis

extra:
  recipe-maintainers:
    - sarnold
