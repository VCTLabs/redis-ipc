{% set name = "redis-ipc" %}
{% set version = "0.0.3" %}
{% set soversion = "0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  path: ..

build:
  number: 0
  skip: true           # [win and float(vc) < 14]
  run_exports:
    - {{ pin_subpackage(name, max_pin='x.x') }}

requirements:
  build:
    - {{ compiler('c') }}  # [not win]
    - {{ compiler('cxx') }}  # [not win]
    - vs2017_win-64  # [win64]
    - vs2017_win-32  # [win32]
    - cmake
    - pkg-config
    - make  # [unix]
    - json-c
    - libhiredis

  host:
    - msinttypes  # [win and float(vc) < 14]
    - redis-py
    - json-c
    - libhiredis
    - pthread-stubs  # [not win]
    - pthreads-win32  # [win]

test:
  commands:
    - ldd ${PREFIX}/lib/libredis_ipc${SHLIB_EXT}  # [linux]
    - test -f ${PREFIX}/include/redis_ipc.h  # [unix]
    - test -f ${PREFIX}/lib/libredis_ipc${SHLIB_EXT}  # [osx]
    - if not exist %LIBRARY_LIB%\redis_ipc.lib exit 1   # [win]
    - if not exist %PREFIX%\\Library\\include\\redis_ipc.h exit 1  # [win]
    - echo "TODO - Pkg tests require redis server."

about:
  home: https://github.com/VCTLabs/redis-ipc
  license: GPL-2.0-only
  license_family: GPL
  license_file: LICENSE
  summary: advanced IPC mechanism using redis

extra:
  recipe-maintainers:
    - sarnold
